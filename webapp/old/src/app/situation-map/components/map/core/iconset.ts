export abstract class Iconset {
    // protected _icons: Array<HTMLImageElement> = [];
    // protected _iconUrls: Array<string> = [];

    constructor() {
        // Initialize icons in memory
        // for (const url of this._iconUrls) {
        //     this._icons.push(new Image());
        //     this._icons[this._icons.length - 1].src = url;
        // }
    }

    // public abstract drawIcon(id: number, data?: any);
}
